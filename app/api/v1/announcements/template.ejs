<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Announcements</title>
    <style>
        html,
        body {
            width: 100%;
            padding: 0;
            margin: 0;
        }

        * {
            /* font-family: "Helvetica Neue"; */
            font-family: sans-serif;
        }
    </style>
</head>

<body>
    <img style="width: 100px; display: block; margin: auto" src="<%=image%>">
    <h1
        style="width: 100%; text-align: center; font-weight: 800; text-decoration: underline; margin-bottom: 20px; margin-top: 20px">
        <strong>Daily Announcements</strong>
    </h1>
    <div style="width: 100%; margin-top: 20px">
        <h3 style="color: #9e9e9e; margin-bottom: 0; margin-top: 0"><strong><%=date%></strong></h3>
        <h3 style="color: #9e9e9e; margin-bottom: 0; margin-top: 0; margin-top: 10px"><strong><%=dayTitle%></strong>
        </h3>
    </div>
    <div style="width: 100%; margin-top: 20px">
        <% for (var i = 0; i < announcement.tiles.length; i++) { %>
        <div>
            <h3 style="color: #781212; text-decoration: underline; margin-bottom: 2px"><%=announcement.tiles[i].title%>
            </h3>
            <% announcement.tiles[i].announcements.forEach(announcement => { %>
            <div style="position: relative">

                <div style="position: absolute; top: 4px; left: 25px; width: 7px; min-width: 7px; height: 7px; min-height: 7px; border-radius: 50%; background-color: black"></div>

                <div style="padding-left: 40px; font-size: 12px">
                    <% let cfg = {}; %>

                    <% let converter = new QuillDeltaToHtmlConverter (announcement.delta, cfg ); %>

                    <% let html = converter.convert (); %>

                    <%-html%>
                </div>

            </div>
            <% }) %>
        </div>
        <% } %>
    </div>
</body>

</html>